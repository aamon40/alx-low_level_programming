#include "main.h"

/**
*_abs - check main
* @r: int input
* Description: This function returns absolute value of a number
* Return: absolute value of number r
*/
int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (r * -1);
}
